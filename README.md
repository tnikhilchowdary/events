
# Node.js Events Example

This project demonstrates how to use the built-in `EventEmitter` module in Node.js.

## 📌 What it does
- Creates a custom event listener for a `response` event.
- Emits the event with dynamic values (name and age).
- Logs the output to the console.

## 🧾 Output Example
Nikhil, Age: 25
